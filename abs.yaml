# the conda-build parameters to use for disabling --skip-existing
build_parameters:
  - "--suppress-variables"
  # - "--error-overlinking" turning off for sake of qt's dependencies

channels:
  - https://staging.continuum.io/prefect/fs/mesalib-feedstock/pr1/482c366
  - https://staging.continuum.io/prefect/fs/qtbase-feedstock/pr7/c308172
