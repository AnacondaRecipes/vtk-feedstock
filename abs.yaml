# the conda-build parameters to use for disabling --skip-existing
build_parameters:
  - "--suppress-variables"
  # - "--error-overlinking" turning off for sake of qt's dependencies

channels:
  - https://staging.continuum.io/prefect/fs/glad-feedstock/pr1/04523de
  - https://staging.continuum.io/prefect/fs/curl-feedstock/pr38/eab1591
